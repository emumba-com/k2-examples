import styled from "styled-components";

export const DrilldownWrapper = styled.div`
  height: 100%;
  position: relative;
  .back-button {
    position: absolute;
    left: 0;
    top: 40px;
    padding: 8px 16px;
    font-weight: bold;
    z-index: 2;
    cursor: pointer;
  }
`;
